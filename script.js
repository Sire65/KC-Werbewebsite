const menuBtn=document.querySelector('.menu-btn');const menu=document.querySelector('nav ul');function closeMenu(){if(menu&&menuBtn){menu.classList.remove('open');menuBtn.setAttribute('aria-expanded','false')}}if(menuBtn&&menu){menuBtn.setAttribute('aria-expanded','false');menuBtn.setAttribute('aria-label','Navigation öffnen');menuBtn.addEventListener('click',()=>{const open=menu.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));menuBtn.setAttribute('aria-label',open?'Navigation schließen':'Navigation öffnen')});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()});document.addEventListener('click',e=>{if(!menu.contains(e.target)&&!menuBtn.contains(e.target))closeMenu()})}document.querySelectorAll('[data-year]').forEach(x=>x.textContent=new Date().getFullYear());const key='kcw-consent-v1';const banner=document.querySelector('#cookieBanner');if(banner&&!localStorage.getItem(key))banner.hidden=false;document.querySelectorAll('[data-consent]').forEach(x=>x.addEventListener('click',()=>{localStorage.setItem(key,x.dataset.consent);if(banner)banner.hidden=true}));document.querySelectorAll('[data-open-consent]').forEach(x=>x.addEventListener('click',e=>{e.preventDefault();if(banner)banner.hidden=false}));